# dsh-deepcompute 插件补丁：把插件行插入 profile 的插件列表。
# 安装：在 profile 目录执行
#   dsh plugin --profile web add link:<本插件路径>
# 注册：把下面这行 insert 追加到 ~/.dsh/cordis.patch.yml（用户补丁层）。
- insert:
    - id: ui-deepcompute
      name: 'dsh-deepcompute'
